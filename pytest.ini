[pytest]
testpaths = tests
addopts = -ra
markers =
    integration: requires a live PostgreSQL (docker compose up -d postgres + alembic upgrade head)
    worker: requires RabbitMQ for broker-level tests; processor/recovery tests can use fakes

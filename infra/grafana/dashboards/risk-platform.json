{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "expr": "sum by (source) (rate(risk_predictions_total[1m]))",
          "legendFormat": "{{source}}"
        }
      ],
      "title": "Predictions per second",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le, source) (rate(risk_prediction_latency_seconds_bucket[5m])))",
          "legendFormat": "p95 {{source}}"
        },
        {
          "expr": "histogram_quantile(0.50, sum by (le, source) (rate(risk_prediction_latency_seconds_bucket[5m])))",
          "legendFormat": "p50 {{source}}"
        }
      ],
      "title": "Prediction latency",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "expr": "sum by (error_type) (increase(risk_prediction_errors_total[5m]))",
          "legendFormat": "{{error_type}}"
        }
      ],
      "title": "Prediction errors",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "pieType": "donut", "tooltip": { "mode": "single" } },
      "targets": [
        {
          "expr": "sum by (risk_level) (risk_predictions_total)",
          "legendFormat": "{{risk_level}}"
        }
      ],
      "title": "Risk level distribution",
      "type": "piechart"
    },
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 8 },
      "id": 5,
      "options": { "legend": { "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "multi" } },
      "targets": [
        {
          "expr": "sum by (status) (risk_batch_jobs_total)",
          "legendFormat": "{{status}}"
        }
      ],
      "title": "Batch jobs by status",
      "type": "barchart"
    }
  ],
  "refresh": "5s",
  "schemaVersion": 40,
  "tags": ["ai-risk-platform"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "AI Risk Platform Observability",
  "uid": "ai-risk-platform",
  "version": 1,
  "weekStart": ""
}
